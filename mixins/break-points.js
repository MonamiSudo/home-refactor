export const breakpoints = {
  data() {
    return {
      picture: { // pitureタグ用のブレイクポイント
        pc: '(max-width: 1024px)',
        tab: '(max-width: 768px)',
        sp: '(max-width: 559px)',
      }
    }
  }
}